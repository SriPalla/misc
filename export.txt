version: '3'

services:
  storage-emulator:
    image: gcr.io/gcp-emulators/storage-emulator:latest
    ports:
      - "8080:8080"  # Local HTTP port for storage emulator
      - "9191:9191"  # Local gRPC port for storage emulator
    environment:
      - CLOUDSDK_CORE_PROJECT=my-test-project  # Set your desired project ID
    command: ["gcloud", "beta", "emulators", "storage", "start", "--host-port=0.0.0.0:9191"]

  minio:
    image: minio/minio
    ports:
      - "9000:9000"  # MinIO Browser GUI port
    environment:
      - MINIO_ACCESS_KEY=minioadmin
      - MINIO_SECRET_KEY=minioadmin
    command: ["server", "/data"]
    volumes:
      - minio-data:/data

volumes:
  minio-data:
